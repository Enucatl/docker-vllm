[project]
name = "vllm-extraction"
version = "0.1.0"
description = "vLLM extraction stack with OpenAI-compatible API"
requires-python = ">=3.8"
dependencies = [
    "openai>=1.0.0",
    "requests>=2.31.0",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
